export interface Teacher {
    id?: number; // The ID might be optional if it's auto-generated on creation.
    name: string;

    // Add any other properties corresponding to the Teacher attributes in your backend.
    // For example:
    // subject?: string;
    // department?: string;
}
